apiVersion: v1
kind: Service
metadata:
  name: {{ template "nginx-demo.fullname" . }}
  labels:
    app: {{ template "nginx-demo.name" . }}
    chart: {{ template "nginx-demo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nginx-demo.name" . }}
    release: {{ .Release.Name }}
